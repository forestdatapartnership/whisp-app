export type PublicConfig = {
  apiUrl: string;
  geometryLimit: number;
  asyncThreshold: number;
  maxUploadFileSizeKb: number | undefined;
  appVersion: string;
  whispPythonVersion: string;
  geoidDefaultCollection: string;
};
